const CreateComment = ({userData}) => {
    return ( 
        <div className="comments create-comment">
            {userData} is currently logged in
        </div>
     );
}
 
export default CreateComment;